---
description: Capture a decision or insight as an atomic note in the memory vault.
---

Capture this as durable memory: **$ARGUMENTS**

1. **Pick the right home:**
   - a lasting design/architecture decision → `memory/decisions/<kebab>.md`
     (use `memory/templates/decision.md`);
   - a reusable concept/principle → `memory/permanent/<kebab>.md`
     (use `memory/templates/default-note.md`);
   - a domain fact → the relevant `memory/domains/*` note;
   - a term definition → `memory/glossary/<term>.md`.
2. **Write it atomically** — one idea, YAML frontmatter, kebab-case filename, and
   **≥2 `[[wikilinks]]`** to related notes (wikilinks, not markdown links).
3. Tell me the path you filed it at. Do **not** commit unless I ask.
